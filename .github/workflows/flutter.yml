name: Flutter CI

on: [push, pull_request]

jobs:
  build:

    runs-on: macOS-latest

    container:
      image: cirrusci/flutter:latest

    test_task:
      pub_cache:
        folder: ~/.pub-cache
      test_script: flutter test
